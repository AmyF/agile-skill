# Git Workflow

Branch format: `{type}/{agile-id}/{slug}`. Commit format: `{type}({agile-id}): {summary}`.

PRs must reference `.agile` artifact paths, traceability, evidence, and gate status. Git and GitHub do not replace `.agile`.
