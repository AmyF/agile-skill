# Installation

Copy `agile-skill/.claude/skills/` into the Claude Skills location. Use `agile-run` to initialize a target project. The target project receives `.agile/`; scripts and schemas stay inside the skill package.

Installable skills after the refactor: `agile-run`, `agile-run-auto`, `agile-tdd`, `agile-git`, and `agile-github`.
