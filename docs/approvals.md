# Approvals

Machine gates may pass automatically. AI must not self-approve semantic meaning, waivers, high-risk changes, or release readiness.

Approval targets must be versioned files, not `latest.md`. Approved artifacts are immutable. Allowed sources include explicit user instruction, batch preauthorization, GitHub PR review, signed decision log entry, and external approval record.
