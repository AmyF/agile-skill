# Merge Gates

Before merge, verify branch name, commit messages, PR metadata, `.agile` traceability, acceptance evidence, and required CI/test evidence. Merge requires explicit approval in `.agile` or current-turn approval. Do not merge around failed or unowned waivers.
