# Authoring Guidelines

Keep `SKILL.md` concise. Put detailed rules in references, generated structures in templates, schemas in schemas, and executable helpers in scripts.

Do not copy skill tooling into `.agile`. Keep artifacts concise and executable.
