# GitHub Integration

GitHub integration is optional and file-based. Modes: Disabled, PR template only, Issue templates only, Issue templates + PR template + Issue/PR workflow, plus optional Actions and Project field template. No API automation.
