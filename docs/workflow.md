# Workflow

Flow: Intake → Feature PRD → Story + Acceptance Criteria → DDD if required → FID → TDD/BDD → Implementation → Acceptance Evidence → Git Gate → Release.

Acceptance Criteria are defined before FID and TDD. BDD belongs to TDD. DDD is risk-based. `.agile` is source of truth.
