# Issue PR Workflow

Recommended controlled sequence:

1. Run `agile-run` gates.
2. Create/update Issue from the feature, story, change, or release artifact.
3. Create PR from the story or feature artifact after Git branch and commit metadata are valid.
4. Merge PR only after gates, CI, and explicit approval.
5. Close Issue only after merge/release acceptance and explicit approval.
