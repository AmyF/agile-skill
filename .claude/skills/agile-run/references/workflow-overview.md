# Workflow Overview

`.agile` is source of truth. Feature, Story, TDD Flow, Release, and Change Request states are enforced by `state-machine.yaml`. BDD belongs to TDD. Mid-workflow changes must be classified before applying.
