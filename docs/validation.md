# Validation

Schemas are machine validation contracts. AI reads references and templates, generates runtime files, runs validation scripts, fixes errors, and reports success only after validation passes. Dependencies: Python 3, PyYAML, jsonschema.
