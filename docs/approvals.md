# Approvals

AI must not self-approve. Approval targets must be versioned files, not `latest.md`. Approved artifacts are immutable. Allowed approval sources include explicit user instruction, GitHub PR review, signed decision log entry, and external approval record.
