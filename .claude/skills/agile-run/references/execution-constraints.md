# Execution Constraints

AI may draft, validate, propose transitions, and record explicit user approvals. AI must not self-approve, infer approval, edit approved versioned artifacts in place, hide drift, create `.github`, run Git/GitHub operations via agile-run, or create scripts/schemas/tools inside `.agile`.
