# Change Requests

Semantic changes to approved artifacts require a Change Request. Each CR has a human-readable `CR-*.md` and machine-readable `CR-*.record.yaml`.

Mid-workflow changes must be classified, impact analyzed, and downstream artifacts marked pending revalidation.
