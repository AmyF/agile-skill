# Installation

Copy `agile-skill/.claude/skills/` into the Claude Skills location. Use `agile-run` to initialize a target project. The target project receives `.agile/`; scripts and schemas stay inside the skill package. GitHub integration is disabled by default and file-based only.
