# Mid-Workflow Change

A mid-workflow change must not be applied silently. Classify it as draft editorial, draft semantic, approved editorial, approved semantic, release freeze, or emergency rollback.

Draft editorial changes can continue with normal gates. Draft semantic changes update traceability and downstream artifacts. Approved semantic changes require a Change Request, impact analysis, and downstream revalidation. Release freeze and emergency rollback changes require explicit owner approval.
